import 'package:flutter/material.dart';

class PuzzleAnimalGlyph extends StatelessWidget {
  const PuzzleAnimalGlyph({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
